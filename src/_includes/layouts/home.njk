{% extends 'layouts/default.njk' %}

{%- block content %}
  <div class="site-title">
    {{ super() }}
    <ul>
      {%- for post in collections.post-%}
      <li>{{ post.data.title }}</li>
      {%- endfor -%}
    </ul>
  </div>
{%- endblock -%}
